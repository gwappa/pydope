{
    "$schema":  "http://json-schema.org/schema#",
    "$comment": "definitions related to the datset repository.",

    "type": "object",

    "properties": {
        "project": { "$ref": "../sections/project.json" },
        "definitions": { "$ref": "../sections/definitions.json" }
    },

    "required": [
        "project",
        "definitions"
    ]
}
